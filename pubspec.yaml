name: apliarte_glass_theme
description: "Material-safe glass theme for Flutter. The default entrypoint preserves Material API compatibility; glass widget wrappers are opt-in."
version: 0.7.0
license: MIT
homepage: https://github.com/erbolamm/apliarte-glass-theme
repository: https://github.com/erbolamm/apliarte-glass-theme
issue_tracker: https://github.com/erbolamm/apliarte-glass-theme/issues

environment:
  sdk: ^3.10.4
  flutter: ">=3.32.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.0.10+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
