name: apliarte_glass_theme_example
description: Demo — one toggle switches between Material and Glass.
publish_to: none

environment:
  sdk: ^3.10.4
  flutter: ">=3.32.0"

dependencies:
  flutter:
    sdk: flutter
  apliarte_glass_theme:
    path: ../

flutter:
  uses-material-design: true
